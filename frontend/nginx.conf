server {
    listen 80;

    location / {
        root   /usr/share/nginx/html;
        index  index.html;
        # Support React client-side routing (SPA fallback)
        try_files $uri $uri/ /index.html;
    }
}
